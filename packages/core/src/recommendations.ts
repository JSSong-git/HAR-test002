/** Re-export rule engine (declarative Fact-Only recommendations). */
export {
  buildRecommendations,
  buildSummaryBullets,
  defaultRules,
  evaluateRules,
  type Rule,
  type RuleContext,
  type RuleResult,
} from "./rules";
